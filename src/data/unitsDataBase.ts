import type { unit } from "../shared/interfaces/units.interface";

let unitsData: { [key: string]: unit[] }[] = [
  { length: [{ name: "meter", value: 1, category: "length" }] },
];

export default unitsData;
